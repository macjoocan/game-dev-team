# 사용법 (game-dev-team 플러그인)

게임 개발 역할 에이전트 팀을 어느 프로젝트에서든 재사용하기 위한 플러그인. 최종 권위자는 항상 사람(당신)이다.

## 1. 설치

### 방법 A — .plugin 파일 (Cowork)
1. 릴리스/배포된 `game-dev-team.plugin` 파일을 Cowork 채팅에 열거나 드래그.
2. 뜨는 설치 버튼을 누른다.

### 방법 B — Claude Code (레포에서 직접)
이 레포를 마켓플레이스로 추가해 설치한다.
```bash
# 예시: 로컬 클론 후 플러그인 디렉터리로 사용하거나
git clone https://github.com/macjoocan/game-dev-team.git
```
그런 다음 Claude Code의 플러그인/마켓플레이스 설정에서 이 디렉터리를 등록한다.
(정확한 등록 방식은 Claude Code 플러그인 문서를 따른다.)

## 2. 새 게임 프로젝트에 팀 세팅
게임 레포에서 Claude에게:
```
게임 팀 세팅해줘
```
→ `setup-game-team` 스킬이 엔진·언어·컨벤션을 물어보고(또는 코드에서 감지) 레포 루트에
`CLAUDE.md`(파이프라인·게이트 규칙)를 생성한다.

설치된 에이전트 확인(Claude Code):
```
/agents
```
pm, game-designer, developer, qa, artist 5개가 보이면 정상.

## 3. 팀 굴리기
첫 실행은 plan 모드로 게이트를 눈으로 확인하는 것을 권장.
```
이 기획을 검토부터 구현까지 진행해줘. 각 게이트는 나한테 확인받고.
```

동작 흐름 (허브-앤-스포크, 각 화살표 = 사람 승인):
```
기획/밸런스(game-designer) → 태스크 분해(pm) → 구현(developer) → 검증(qa) → 아트(artist)
```
- 에이전트끼리 직접 대화하지 않는다. 오케스트레이터(메인 세션)가 결과를 모아 다음 역할로 넘긴다.
- 각 단계 전환은 사람 승인을 거친다.

## 4. 역할별 호출 예시
- `game-designer로 이 기획서 검수해줘` — 누락·미결·근거 없는 추정값 점검
- `pm으로 이 변경을 태스크로 분해해줘`
- `developer로 이 기능 프로토 만들어줘` (worktree 격리로 작업)
- `qa로 이 변경 리뷰하고 회귀 확인해줘` — 통과/조건부/반려 판정
- `artist로 카드 UI 시안 2~3안 만들어줘`

## 5. 권장 확장 (선택)
개발·QA·아트가 실제 도구를 직접 다루게 하려면 MCP를 연결:
- 엔진: Unity MCP / Unreal MCP / Godot MCP
- 3D 아트: Blender MCP
- 트래커: GitHub · Linear · Notion

## 6. 게이트 체크(사람용 요약)
기획 통과 조건: 근거 없는 수치 0 · 미결 명시 · 핵심 수치 공란 0.
구현/머지 전: 콘솔·빌드 에러 0, 기존 동작 회귀 없음, QA 판정 통과.
